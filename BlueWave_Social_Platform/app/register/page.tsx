'use client';import{useState}from'react';import{useRouter}from'next/navigation';import Link from'next/link';
export default function Register(){const r=useRouter();const[n,setN]=useState('');const[email,setEmail]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');async function go(x:any){x.preventDefault();setE('');const z=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:n,email,password:p})});const d=await z.json();if(!z.ok)return setE(d.error);r.push('/home')}return <main className="auth"><form className="card" onSubmit={go}><div className="brand">Qikly</div><h2>Create your account</h2><input className="input" placeholder="Name" value={n} onChange={e=>setN(e.target.value)} required/><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" placeholder="Password (6+ chars)" type="password" value={p} onChange={e=>setP(e.target.value)} required minLength={6}/>{e&&<p className="muted">{e}</p>}<button className="btn" style={{width:'100%'}}>Join Qikly</button><p className="muted">Already have an account? <Link href="/login">Log in</Link></p></form></main>}

'use client';import{useEffect,useState}from'react';
export default function Notifications(){const[n,setN]=useState<any[]>([]);useEffect(()=>{fetch('/api/notifications').then(x=>x.json()).then(d=>setN(d.notifications||[]))},[]);return <><header className="top"><a className="brand" href="/home">Qikly</a><a href="/home">Home</a></header><main className="layout" style={{gridTemplateColumns:'1fr'}}><section className="panel"><h2>Notifications</h2>{n.length?n.map(x=><div className="userrow" key={x.id}>🔔 <span>{x.fromName} {x.type==='follow'?'followed you.':'interacted with you.'}</span></div>):<p className="muted">No notifications yet.</p>}</section></main></>}

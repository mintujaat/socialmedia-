'use client';import{useEffect,useState}from'react';import Link from'next/link';export default function Admin(){const[p,setP]=useState<any[]>([]);async function load(){const d=await fetch('/api/posts').then(x=>x.json());setP(d.posts||[])}useEffect(()=>{load()},[]);async function del(id:string){await fetch('/api/admin/posts',{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({id})});load()}return <><header className="top"><Link className="brand" href="/home">Qikly Admin</Link><Link href="/home">Back</Link></header><main className="layout" style={{gridTemplateColumns:'1fr'}}><section><h2>Post moderation</h2>{p.map(x=><article className="post" key={x.id}><b>{x.authorName}</b><p>{x.text}</p>{x.imageUrl&&<img className="postimg" src={x.imageUrl}/>}<button className="pill danger" onClick={()=>del(x.id)}>Delete post</button></article>)}</section></main></>}

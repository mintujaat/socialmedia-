'use client';import {useState}from'react';import{useRouter}from'next/navigation';import Link from'next/link';
export default function Login(){const r=useRouter();const[email,setEmail]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');async function go(x:any){x.preventDefault();setE('');const z=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password:p})});const d=await z.json();if(!z.ok)return setE(d.error);r.push('/home');r.refresh()}return <main className="auth"><form className="card" onSubmit={go}><div className="brand">Qikly</div><h2>Welcome back</h2><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" placeholder="Password" type="password" value={p} onChange={e=>setP(e.target.value)} required/>{e&&<p className="muted">{e}</p>}<button className="btn" style={{width:'100%'}}>Log in</button><p className="muted">New here? <Link href="/register">Create account</Link></p></form></main>}

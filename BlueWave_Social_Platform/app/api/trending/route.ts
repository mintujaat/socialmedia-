import{db}from'@/lib/firebase-admin';import{ok,fail}from'@/lib/response';export async function GET(){try{const s=await db.collection('posts').orderBy('createdAt','desc').limit(100).get();const c:Record<string,number>={};s.docs.forEach(d=>{const t=(d.data().text||'').match(/#[\p{L}\p{N}_]+/gu)||[];t.forEach((x:string)=>c[x.toLowerCase()]=(c[x.toLowerCase()]||0)+1)});return ok({hashtags:Object.entries(c).sort((a,b)=>b[1]-a[1]).slice(0,10).map(([tag,count])=>({tag,count}))})}catch(e:any){return fail(e.message,500)}}

'use client';import{useEffect,useState}from'react';import Link from'next/link';export default function Explore(){const[q,setQ]=useState('');const[u,setU]=useState<any[]>([]);const[t,setT]=useState<any[]>([]);const[tr,setTr]=useState<any[]>([]);async function search(){const d=await fetch('/api/users/search?q='+encodeURIComponent(q)).then(x=>x.json());setU(d.users||[])}useEffect(()=>{fetch('/api/posts').then(x=>x.json()).then(d=>setT(d.posts||[]));fetch('/api/trending').then(x=>x.json()).then(d=>setTr(d.hashtags||[]))},[]);return <><header className="top"><Link className="brand" href="/home">Qikly</Link><Link href="/home">Home</Link></header><main className="layout" style={{gridTemplateColumns:'1fr'}}><section><input className="search" placeholder="Search people by username…" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()}/>{u.map(x=><div className="userrow" key={x.uid}><img className="avatar" src={x.photoURL||'/avatar.svg'}/><Link href={'/profile/'+x.uid}><b>{x.name}</b><div className="muted">@{x.username}</div></Link></div>)}<h2>Trending hashtags</h2><div className="row" style={{justifyContent:'flex-start'}}>{tr.map(x=><span className="iconbtn" key={x.tag}>{x.tag} · {x.count}</span>)}</div><h2>Explore</h2>{t.map(p=><div className="post" key={p.id}><b>#{(p.text.match(/#[\w]+/g)||[])[0]?.slice(1)||'qikly'}</b><p>{p.text}</p>{p.imageUrl&&<img className="postimg" src={p.imageUrl}/>}</div>)}</section></main></>}

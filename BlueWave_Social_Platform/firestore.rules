// All application reads/writes go through Next.js server routes using Firebase Admin.
// Admin SDK bypasses Firestore rules. Keep this locked down if clients should not access Firestore directly.
rules_version = '2';
service cloud.firestore { match /databases/{database}/documents { match /{document=**} { allow read, write: if false; } } }
